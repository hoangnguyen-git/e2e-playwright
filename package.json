{
  "name": "e2e-playwright",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test test.spec.ts --project=chromium --headed",
    "test:docker": "npx playwright test test.spec.ts --project=chromium",
    "report:open": "npx allure generate --clean allure-results && npx allure open",
    "report": "npx allure generate --clean allure-results"
  },
  "keywords": [],
  "author": "Hoang Van",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.36.1",
    "@types/adm-zip": "^0.5.0",
    "allure-commandline": "^2.23.0",
    "allure-playwright": "^2.4.0",
    "typescript": "^5.1.6",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "rimraf": "^5.0.1"
  }
}
